{
  "name": "daily-report-reply-hub-netlify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node --check assets/app.js && node --check netlify/functions/reports.mjs",
    "test": "npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  }
}
